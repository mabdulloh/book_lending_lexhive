spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    clean-disabled: false

app:
  jwt:
    secret: test-secret-must-be-at-least-32-bytes-long-for-hs256
    ttl-minutes: 60
  borrowing:
    max-active-loans-per-member: 3
    loan-duration-days: 14
  seed:
    admin:
      username: admin
      password: admin123
    member:
      email: member@example.com
      name: Demo Member
      password: member123

logging:
  level:
    io.github.mabdulloh.booklending: DEBUG